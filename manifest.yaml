---
applications:
  - name: btp-pg-socks
    memory: 128M
    services:
      - connectivity
    env:
      PG_CONNECTIVITY_ENABLED: true
      PG_CONNECTIVITY_LOCATION_ID: 'pieter-nas'
      PG_HOST: 'local-pg'
      PG_PORT: 5433
      PG_SSL: false
      PG_DB: 'bookshop'
      PG_USERNAME: 'postgres'
      PG_PASSWORD: 'secret'
